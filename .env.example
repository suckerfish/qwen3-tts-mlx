# Qwen3-TTS MCP Server Configuration

# URL of the FastAPI TTS backend.
# macOS Docker Desktop: http://host.docker.internal:8000 (default)
# Linux Docker: http://172.17.0.1:8000 (use host gateway IP)
TTS_API_URL=http://host.docker.internal:8000

# Port the MCP server listens on inside the container (mapped to host).
MCP_PORT=8080

# Logging level: DEBUG, INFO, WARNING, ERROR
LOG_LEVEL=INFO
